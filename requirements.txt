# slide-svg — root-level entry for `pip install -r requirements.txt`.
# Canonical source of truth: .claude/skills/slide/requirements.txt
# (`/slide` 스킬이 직접 참조하므로 의존성 변경은 그쪽에서 한다.)
-r .claude/skills/slide/requirements.txt
